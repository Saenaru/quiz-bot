# Quiz Bot

Бот для проведения викторин с вопросами из текстовых файлов. Пользователи могут получать случайные вопросы, отвечать на них и отслеживать свою статистику.

## Ссылки на ботов

### 🤖 Telegram бот
**[Telegram: @QuizTGtestbot](https://t.me/QuizTGtestbot)** - ищите `@QuizTGtestbot` в Telegram

### 👥 VK бот
**[Группа VK с ботом](https://vk.com/club232927626)** - напишите "Начать" в сообщениях группы


## Структура проекта
```sh
/opt/quiz-bot/
├── tg_bot.py        # Основной скрипт Telegram бота
├── vk_bot.py        # Основной скрипт VK бота
├── database.py      # Общие функции работы с Redis
├── parser.py        # Скрипт для импорта вопросов в Redis
├── quiz-questions/  # Папка с текстовыми файлами викторин
├── requirements.txt # Зависимости
└── .env             # Токены и настройки подключения
```

## 1. Клонирование и установка зависимостей
```sh
# Клонировать репозиторий
git clone <repository-url>
cd quiz-bot

# Создать виртуальное окружение
python -m venv venv

# Активировать виртуальное окружение
# Для Windows:
venv\Scripts\activate
# Для Linux/Mac:
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

## 2. Настройка Redis и .env
Убедитесь, что у вас установлен Redis. Создайте файл .env:

```sh
TG_TOKEN=ваш_токен_телеграм
VK_TOKEN=ваш_токен_вк
REDIS_HOST=localhost
REDIS_PORT=6379
```

## 3. Импорт вопросов
Перед запуском ботов необходимо загрузить вопросы в Redis из папки quiz-questions:

```sh
python parser.py --path quiz-questions
```

## 4.Запуск бота

```sh
python tg_bot.py
python vk_bot.py
```
